import React from 'react';
import './App.css';

class App extends React.Component {
  // state = {
  //   currentPage: "home"
  // };

  // navigateTo = (page) => { 
  //   this.setState({currentPage:page});
  // }
  
  render() { 
    return <>App</>;
  }
}

export default App;
